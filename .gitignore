node_modules/
*.log
# health-record outputs written into scanned repos during local demos.
# (promoted rules under .tokenclinic/rules/ ARE committed — they're the asset.)
fixtures/**/.tokenclinic/history.jsonl
fixtures/**/.tokenclinic/profile.json
fixtures/**/.tokenclinic/quarantine/
